# React Native Version

This folder is reserved for the React Native version of the Flashcard project.

Place the React Native app source files here, for example:
- `App.tsx`
- `package.json`
- `tsconfig.json`
- `src/`

You can use this directory to keep the mobile version separate from the web app.
